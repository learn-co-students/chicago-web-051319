<% if @errors %>
  <% @errors.each do |error_msg| %>
    <p><%= error_msg %></p>
  <% end %>
<% end %>

<%= form_for(@cohort) do |cohort_form| %>
  <%= cohort_form.label :name, "Cohort Name" %>
  <%= cohort_form.text_field :name %>
  <%= cohort_form.submit "Make One!" %>
<% end %>
